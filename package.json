{
  "name": "video-streaming-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-install": "cd frontend && npm install",
    "vercel-build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
} 
